import React from 'react';
import ReactDOM from 'react-dom/client';
import DesligamentoApp from './DesligamentoApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DesligamentoApp />
  </React.StrictMode>
);
