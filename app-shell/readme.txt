Resumo dos passos

Rode npm run build em cada microfrontend.

Rode npm run preview e veja qual porta está sendo usada.

Atualize os remotes no app-shell com a porta correta do remoteEntry.js de cada microfrontend.

Rode o app-shell com vite dev ou vite preview ou app-shell> npm run dev 